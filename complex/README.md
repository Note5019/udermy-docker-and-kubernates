### Folder Structure
```
complex
├── client (React server)
│  └── dockerfile.dev
├── server (Express server)
└── worker (Worker)
```
---
Used Commands.

` docker build -f Dockerfile.dev . `

` docker run {image_id}`